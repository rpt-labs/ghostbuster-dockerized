import { Component } from 'react';

class Cache extends Component {
  state = {
    cache: false
  };

  handleToggle() {
   // change state.cache upon button toggle
  }

  render() {
    return(
      // toggle + label + "last update..."
    );
  }

}